# Contains session cookies / auth headers captured for debugging - never commit.
curl-and-selectors.txt

# Untracked scratch/reference files not part of the tool itself.
broken-link.txt
wymiana.txt
Photos/
code/

# multi-market runner: live browser session (never commit - equivalent to a password)
# and per-run logs (order numbers / issue links, not needed in version control).
multi-market/browser-profile/
multi-market/logs/
multi-market/__pycache__/
